# POC: Secure Voting Architecture
(Proof of Concept for CD Voting System)

เอกสารฉบับนี้อธิบายแนวทางการแก้ไขช่องโหว่ Critical โดยเปลี่ยนสถาปัตยกรรมจาก Client-Side Logic มาเป็น **Server-Side Authority** ร่วมกับ Database จริง (MySQL)

## 📂 โครงสร้างไฟล์ POC
- **`app/poc/schema.sql`**: ออกแบบตาราง Database ใหม่เพื่อรองรับความลับในการโหวต (Anonymous Voting)
- **`app/poc/lib/db.ts`**: จำลองการเชื่อมต่อ Database และ Transaction (Mock Layer)
- **`app/poc/actions.ts`**: Server Actions ที่ทำหน้าที่เป็น Gatekeeper ตรวจสอบสิทธิ์และบันทึกข้อมูล
- **`app/poc/page.tsx`**: หน้า UI ตัวอย่างที่เรียกใช้ Server Actions

---

## 🔒 หลักการความปลอดภัยที่นำมาใช้

### 1. Split Knowledge (การแยกข้อมูลเพื่อความลับ)
ปัญหาเดิมคือตาราง `VoteRecord` เก็บทั้ง `Student ID` และ `Vote Result` ไว้ด้วยกัน ทำให้รู้ว่าใครเลือกใคร
**วิธีแก้:** เราแยกตารางออกเป็น 2 ส่วนที่ไม่มีความสัมพันธ์กันโดยตรง:
1.  **`vote_history`**: เก็บแค่ว่า "นักเรียน ID นี้ มาใช้สิทธิ์แล้ว" (เพื่อกันการโหวตซ้ำ)
2.  **`votes`**: เก็บแค่ "คะแนนของเบอร์ไหน" (ไม่มี ID นักเรียน)

### 2. Server-Side Validation & Authority
ปัญหาเดิมคือการตรวจสอบสิทธิ์ (เช่น ห้ามโหวตซ้ำ) ทำที่ JavaScript ฝั่ง Client ซึ่งแก้ได้ง่าย
**วิธีแก้:** ย้าย Logic ทั้งหมดไปไว้ที่ `actions.ts` (Server Action)
- Client มีหน้าที่แค่ **ส่งคำร้อง (Request)**
- Server มีหน้าที่ **ตรวจสอบ (Verify)** และ **ตัดสินใจ (Execute)**
- ใช้ `HttpOnly Cookie` ในการระบุตัวตนแทน localStorage (กัน XSS/Manipulation)

### 3. Database Transactions (ความถูกต้องของข้อมูล)
การบันทึกข้อมูล 2 ตาราง (`vote_history` และ `votes`) ต้องเกิดขึ้นพร้อมกัน ถ้าอันใดอันหนึ่งพัง ต้องยกเลิกทั้งหมด (Rollback) เพื่อป้องกันปัญหาคะแนนเกินหรือบัตรเขย่ง
- ดูตัวอย่างโค้ดใน `castVoteAction` ในไฟล์ `app/poc/actions.ts`

---

## 🛠️ ขั้นตอนการนำไปใช้กับ MySQL โรงเรียน

1.  **รัน Script SQL:** นำไฟล์ `schema.sql` ไปรันใน MySQL Server ของโรงเรียนเพื่อสร้างตาราง
2.  **ติดตั้ง Driver:** `npm install mysql2`
3.  **แก้ไข `lib/db.ts`:** เปลี่ยนจาก Mock Class เป็น `mysql2/promise` connection pool
    ```typescript
    import mysql from 'mysql2/promise';
    export const db = mysql.createPool({ ...config });
    ```
4.  **เปลี่ยน Hash Function:** ใน `actions.ts` ตอน Login ควรใช้ `bcrypt` หรือ `argon2` ในการตรวจสอบรหัสผ่าน/เลขบัตรประชาชน แทนการส่ง Text ธรรมดา

---

## ⚠️ สิ่งที่ต้องระวังเพิ่มเติม
- **SQL Injection:** การเขียน Query ต้องใช้ Prepared Statements (`?`) เสมอ (ใน POC ทำให้ดูเป็นตัวอย่างแล้ว)
- **Rate Limiting:** ควรจำกัดจำนวนครั้งการ Login ผิดพลาด เพื่อป้องกัน Brute Force
